# OS / editor
.DS_Store
.idea/
*.swp

# Node / front (front has its own .gitignore too)
node_modules/
front/dist/

# Ruby / back has its own .gitignore (log, tmp, storage, master.key…)

# NOTE: front/src/routeTree.gen.ts is intentionally committed so a fresh
# clone can typecheck/build before the Vite dev server has run once.

# Secrets locaux
.env
.env.local
